<x-layouts.admin title="Notifikasi"><div class="mx-auto max-w-[1100px] space-y-3">@forelse($items as $item)<article class="rounded-md border border-black bg-white p-3"><h2 class="text-lg font-bold">{{ $item->title }}</h2><p class="mt-2 text-[15px]">{{ $item->message }}</p><time class="mt-2 block text-xs text-slate-500">{{ $item->created_at->format('d M Y, H:i') }}</time></article>@empty<p class="rounded border bg-white p-8 text-center text-slate-500">Belum ada notifikasi.</p>@endforelse</div></x-layouts.admin>
